# Model artifacts directory
# Saved by train_pipeline.py after a successful training run:
#   - churn_model_latest.pkl        — trained (and calibrated) model
#   - categorical_encoders.pkl      — fitted encoding pipeline
#   - feature_names.json            — list of feature columns in training order
#   - model_metadata.json           — algorithm, metrics, version info
